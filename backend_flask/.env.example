# ========== DATABASE CONFIGURATION ==========
# Copy this file and rename it as .env
# Then replace the values with your real credentials

DB_HOST=localhost
DB_USER=your_mysql_user
DB_PASSWORD=your_mysql_password
DB_NAME=artify_db